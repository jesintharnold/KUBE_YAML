apiVersion: v1
kind: Service
metadata:
  name: voting
  labels:
    name: voting-service
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30004
  selector:
    app:  voting-app
    name: voting-pod
